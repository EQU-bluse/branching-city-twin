[project]
name = "branching-city-twin"
version = "0.1.0"
description = "Branching and replayable city digital twin backend"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
